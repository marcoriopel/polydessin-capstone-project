export interface DrawingData {
    id: string;
    drawingPng: string;
    name: string;
    tags: string[];
}

export interface MetaData {
    id: string;
    name: string;
    tags: string[];
}

export interface Drawing {
    id: string;
    drawingPng: string;
}
